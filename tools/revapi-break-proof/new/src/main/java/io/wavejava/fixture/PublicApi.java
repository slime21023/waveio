package io.wavejava.fixture;

/** Version 1.0.1 of the proof API; the {@code removed()} member is intentionally absent. */
public final class PublicApi {
    /** Member retained by the candidate version. */
    public String retained() {
        return "retained";
    }
}
