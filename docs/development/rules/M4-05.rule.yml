goal: "Provide a memory-only handler fixture built on production execution and Task semantics"
keyresults:
  - kr-1: "Fixture opens no socket and invokes handler through Task.run and ExecutionRuntime."
  - kr-2: "Fixture exposes the committed response transaction to tests."
  - kr-3: "Fixture requires explicit request, registry, handler, and execution configuration."
rules:
  - id: m4-05-no-socket-fixture
    name: handler-fixture-is-memory-only
    apply: "waveio-http/src/main/java/io/waveio/http/InMemoryExchange.java"
    level: error
    desc:
      - "The fixture owns no transport or socket dependency."
