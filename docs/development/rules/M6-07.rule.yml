goal: "Close the public server facade consistency gate"
keyresults:
  - kr-1: "Public startup and test support do not expose transport internals."
  - kr-2: "Server resource limits and timeouts remain explicit in ServerSpec."
  - kr-3: "Examples, roadmap, and implementation progress agree."
rules:
  - id: m6-07-no-implicit-server-defaults
    name: public-server-configuration-is-explicit
    apply: "waveio-server/src/main/java"
    level: error
    desc:
      - "The public server facade must not introduce implicit capacity or timeout defaults."
