<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.wavejava</groupId>
    <artifactId>wave-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>wave build</name>
    <description>Build parent for the wave HTTP application framework.</description>

    <modules>
        <module>wave</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <wave.transport.tests>false</wave.transport.tests>
        <wave.reliability.tests>false</wave.reliability.tests>
        <wave.reliability.excludedGroups>reliability</wave.reliability.excludedGroups>
        <wave.distribution.outputTimestamp>2026-01-01T00:00:00Z</wave.distribution.outputTimestamp>
        <netty.leak-detection.level>simple</netty.leak-detection.level>

        <netty.version>4.1.137.Final</netty.version>
        <jackson.version>2.22.2</jackson.version>
        <slf4j.version>2.0.19</slf4j.version>
        <junit.version>5.14.4</junit.version>
        <archunit.version>1.5.0</archunit.version>

        <maven.compiler-plugin.version>3.13.0</maven.compiler-plugin.version>
        <maven.enforcer-plugin.version>3.5.0</maven.enforcer-plugin.version>
        <maven.surefire-plugin.version>3.6.0</maven.surefire-plugin.version>
        <maven.failsafe-plugin.version>3.6.0</maven.failsafe-plugin.version>
        <revapi-maven-plugin.version>0.15.1</revapi-maven-plugin.version>
        <revapi-java.version>0.28.2</revapi-java.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler-plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>${maven.compiler.parameters}</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire-plugin.version}</version>
                    <configuration>
                        <useModulePath>true</useModulePath>
                        <failIfNoTests>false</failIfNoTests>
                        <excludedGroups>${wave.reliability.excludedGroups}</excludedGroups>
                        <systemPropertyVariables>
                            <wave.transport.tests>${wave.transport.tests}</wave.transport.tests>
                            <wave.reliability.tests>${wave.reliability.tests}</wave.reliability.tests>
                            <io.netty.leakDetection.level>${netty.leak-detection.level}</io.netty.leakDetection.level>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven.failsafe-plugin.version}</version>
                    <configuration>
                        <useModulePath>true</useModulePath>
                        <failIfNoTests>false</failIfNoTests>
                        <systemPropertyVariables>
                            <wave.transport.tests>${wave.transport.tests}</wave.transport.tests>
                            <wave.reliability.tests>${wave.reliability.tests}</wave.reliability.tests>
                            <io.netty.leakDetection.level>${netty.leak-detection.level}</io.netty.leakDetection.level>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <outputTimestamp>${wave.distribution.outputTimestamp}</outputTimestamp>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven.enforcer-plugin.version}</version>
                <configuration>
                    <rules>
                        <requireMavenVersion>
                            <version>[3.9.0,)</version>
                        </requireMavenVersion>
                        <requireJavaVersion>
                            <version>[25,26)</version>
                        </requireJavaVersion>
                        <dependencyConvergence/>
                        <banDuplicatePomDependencyVersions/>
                    </rules>
                </configuration>
                <executions>
                    <execution>
                        <id>enforce-build-baseline</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
